<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://farzanehtatari.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://farzanehtatari.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-08-22T17:12:41+00:00</updated><id>https://farzanehtatari.github.io/feed.xml</id><title type="html">blank</title><subtitle>Personal academic site — control systems, machine learning, and reinforcement learning for automotive and robotics applications. </subtitle><entry><title type="html">From DDPG to SAC: a lineage of fixes</title><link href="https://farzanehtatari.github.io/blog/2026/ddpg-td3-sac-lineage/" rel="alternate" type="text/html" title="From DDPG to SAC: a lineage of fixes"/><published>2026-08-21T00:00:00+00:00</published><updated>2026-08-21T00:00:00+00:00</updated><id>https://farzanehtatari.github.io/blog/2026/ddpg-td3-sac-lineage</id><content type="html" xml:base="https://farzanehtatari.github.io/blog/2026/ddpg-td3-sac-lineage/"><![CDATA[<p>Most tutorials present DDPG, TD3, and SAC as three separate continuous-control algorithms you choose between. That framing hides the useful part. They’re better read as <strong>one algorithm and two rounds of debugging</strong> — each successor exists because someone diagnosed a specific, nameable failure in its predecessor and fixed it.</p> <p>If you understand what broke, you understand why the fixes look the way they do. And you get a much better sense of when you can get away with the simpler thing.</p> <hr/> <h2 id="ddpg-the-original-bet">DDPG: the original bet</h2> <p>DDPG (Lillicrap et al., 2015) took DQN’s machinery — replay buffer, target networks — and made it work for continuous actions.</p> <p>The problem with continuous actions is that DQN’s <code class="language-plaintext highlighter-rouge">argmax_a Q(s,a)</code> is intractable when <code class="language-plaintext highlighter-rouge">a</code> is a vector of real numbers. DDPG’s answer: train a <strong>deterministic actor</strong> <code class="language-plaintext highlighter-rouge">μ(s)</code> to output the action the critic likes best, and update it by pushing its parameters along <code class="language-plaintext highlighter-rouge">∇_a Q(s,a)</code> — the deterministic policy gradient.</p> <p>The pieces:</p> <ul> <li><strong>Critic</strong> <code class="language-plaintext highlighter-rouge">Q(s,a)</code> trained on TD targets <code class="language-plaintext highlighter-rouge">y = r + γ Q'(s', μ'(s'))</code></li> <li><strong>Actor</strong> <code class="language-plaintext highlighter-rouge">μ(s)</code> trained to maximize <code class="language-plaintext highlighter-rouge">Q(s, μ(s))</code></li> <li><strong>Target networks</strong> <code class="language-plaintext highlighter-rouge">Q'</code>, <code class="language-plaintext highlighter-rouge">μ'</code> updated by slow Polyak averaging (<code class="language-plaintext highlighter-rouge">τ ≈ 0.005</code>)</li> <li><strong>Replay buffer</strong> for off-policy sample reuse</li> <li><strong>Exploration noise</strong> added to actions at collection time, since <code class="language-plaintext highlighter-rouge">μ</code> is deterministic and would otherwise never explore</li> </ul> <p>It works. It also has a reputation for being miserable to tune, and that reputation is deserved.</p> <hr/> <h2 id="what-actually-goes-wrong-in-ddpg">What actually goes wrong in DDPG</h2> <p>Two failures, and they compound.</p> <p><strong>Overestimation bias.</strong> The TD target contains an implicit maximization: the actor is trained to find high-<code class="language-plaintext highlighter-rouge">Q</code> actions, so <code class="language-plaintext highlighter-rouge">Q(s, μ(s))</code> sits near the top of the critic’s estimate. Function approximation error is noise on top of the true value — and taking a near-max over a noisy estimate is biased upward. That inflated value goes into the next TD target, the critic learns it, the actor chases it further. The bias doesn’t wash out; it accumulates.</p> <p>You can watch this happen. Log your critic’s predicted <code class="language-plaintext highlighter-rouge">Q</code> against the discounted return actually achieved. In a failing DDPG run, the prediction drifts steadily above the truth and never comes back.</p> <p><strong>The actor exploits the critic’s errors.</strong> The actor’s whole job is to find the argmax of the critic. If the critic has a spurious bump — a region of action space it wrongly thinks is great, usually because it has little data there — the actor will find it and go there. This is adversarial in a way that’s easy to miss: the actor is <em>optimizing against the critic’s mistakes</em>.</p> <p>Add a deterministic policy whose exploration comes entirely from externally injected noise, and you have something that’s very sensitive to the noise schedule, the learning rates, and the random seed.</p> <hr/> <h2 id="td3-three-fixes-for-the-bias">TD3: three fixes for the bias</h2> <p>TD3 (Fujimoto, van Hoof &amp; Meger, 2018) is DDPG plus three modifications. The paper’s title is refreshingly literal — <em>Addressing Function Approximation Error in Actor-Critic Methods</em>.</p> <p><strong>1. Clipped double Q-learning.</strong> Train two independent critics. Build the TD target from the <em>smaller</em> of the two:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>y = r + γ · min( Q'₁(s', ã), Q'₂(s', ã) )
</code></pre></div></div> <p>Taking the minimum introduces a downward bias, which is the point — underestimation doesn’t compound the way overestimation does. An underestimated action just doesn’t get selected; it doesn’t poison future targets.</p> <p><strong>2. Delayed policy updates.</strong> Update the actor once every <code class="language-plaintext highlighter-rouge">d</code> critic updates (<code class="language-plaintext highlighter-rouge">d = 2</code> in the paper). The reasoning: if the actor chases a critic that’s still moving, it chases noise. Let the value estimate settle first. This is the same instinct as timescale separation in adaptive control — the inner loop should converge faster than the outer one.</p> <p><strong>3. Target policy smoothing.</strong> Add clipped noise to the action used in the TD target:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ã = μ'(s') + ε,    ε ~ clip( N(0, σ), −c, c )
</code></pre></div></div> <p>This enforces a smoothness prior: nearby actions should have similar values. It flattens exactly the narrow spurious peaks the actor would otherwise exploit.</p> <p>TD3 is substantially more stable than DDPG. But exploration is still extrinsic — you’re still hand-tuning an external noise process, and the policy is still deterministic.</p> <hr/> <h2 id="sac-change-the-objective-instead">SAC: change the objective instead</h2> <p>SAC (Haarnoja et al., 2018) attacks the problem from a different direction. Rather than patching a deterministic policy, it changes what “optimal” means.</p> <p>The maximum-entropy objective adds a policy-entropy term to the reward:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>J(π) = Σ_t E[ r(s_t, a_t) + α · H( π(·|s_t) ) ]
</code></pre></div></div> <p>The agent is now rewarded for <strong>acting as randomly as it can while still doing well</strong>. Three things follow, and they’re all consequences of that one change:</p> <p><strong>Exploration becomes intrinsic.</strong> No injected noise, no schedule to tune. The policy stays stochastic because staying stochastic is <em>worth reward</em>. When a region of state space has multiple near-equally-good actions, SAC keeps its options open there instead of prematurely committing.</p> <p><strong>Brittleness drops.</strong> A stochastic policy can’t sit on a razor-thin spike in the critic — the entropy term pushes it toward broad, flat optima. Those generalize better and survive small model mismatch, which matters enormously when you eventually deploy on hardware that isn’t your simulator.</p> <p><strong>The temperature <code class="language-plaintext highlighter-rouge">α</code> becomes the knob that matters.</strong> Too high and the policy is aimlessly random; too low and you’re back to a deterministic policy with all of DDPG’s problems. This was SAC’s main tuning burden until the 2019 follow-up made <code class="language-plaintext highlighter-rouge">α</code> automatically adjusted — you specify a target entropy (commonly <code class="language-plaintext highlighter-rouge">−dim(A)</code>) and <code class="language-plaintext highlighter-rouge">α</code> is tuned by dual gradient descent to hit it. Use the auto-tuning version. There is no good reason not to.</p> <p>SAC also keeps TD3’s twin critics with the <code class="language-plaintext highlighter-rouge">min</code>. The bias problem didn’t go away; it’s orthogonal to the entropy idea.</p> <hr/> <h2 id="reading-it-as-a-sequence">Reading it as a sequence</h2> <table> <thead> <tr> <th> </th> <th>Exploration</th> <th>Bias control</th> <th>Policy</th> </tr> </thead> <tbody> <tr> <td><strong>DDPG</strong></td> <td>injected noise</td> <td>none</td> <td>deterministic</td> </tr> <tr> <td><strong>TD3</strong></td> <td>injected noise</td> <td>twin critics, delay, smoothing</td> <td>deterministic</td> </tr> <tr> <td><strong>SAC</strong></td> <td>intrinsic (entropy)</td> <td>twin critics</td> <td>stochastic</td> </tr> </tbody> </table> <p>TD3 fixes DDPG’s <em>value estimation</em>. SAC fixes DDPG’s <em>exploration and brittleness</em>, and inherits TD3’s value fix along the way. They’re not competitors so much as two different diagnoses of the same patient, and SAC happens to have taken the other one’s medicine too.</p> <hr/> <h2 id="a-practitioners-note">A practitioner’s note</h2> <p>I used SAC as one of the components in a <a href="https://arxiv.org/abs/2608.17258">hybrid controller for vehicle lateral control</a> — blending a learned policy with a constrained MPC. A few things I’d tell my earlier self:</p> <p><strong>Report multiple seeds or don’t report.</strong> Single-seed continuous-control results are close to meaningless. My own numbers are mean ± std over five training seeds, and the spread was large enough that any single run would have told a misleading story. This is the single most common flaw in RL results I read.</p> <p><strong>SAC’s stability doesn’t transfer to your reward function.</strong> SAC is robust to hyperparameters in a way DDPG isn’t. It is not robust to a reward that accidentally makes a degenerate behavior optimal. Most of my debugging time went into the reward, not the algorithm.</p> <p><strong>Watch the entropy, not just the return.</strong> Entropy collapsing to near-zero early usually means <code class="language-plaintext highlighter-rouge">α</code> is too low and you’ve quietly reverted to a deterministic policy. Entropy staying pinned high means the task reward isn’t outweighing the entropy bonus. The return curve alone won’t tell you which failure you have.</p> <p><strong>Understand what “it works” means before deploying.</strong> SAC gave the best tracking error in my comparison. It also gives no guarantee whatsoever about what it will command in a state it hasn’t seen. That gap between “best average performance” and “acceptable worst case” is the whole reason for pairing it with a model-based controller — but that’s the next post.</p> <hr/> <h2 id="references">References</h2> <ul> <li>Lillicrap et al., <em>Continuous Control with Deep Reinforcement Learning</em>, ICLR 2016. <a href="https://arxiv.org/abs/1509.02971">arXiv:1509.02971</a></li> <li>Fujimoto, van Hoof &amp; Meger, <em>Addressing Function Approximation Error in Actor-Critic Methods</em>, ICML 2018. <a href="https://arxiv.org/abs/1802.09477">arXiv:1802.09477</a></li> <li>Haarnoja et al., <em>Soft Actor-Critic: Off-Policy Maximum Entropy Deep RL with a Stochastic Actor</em>, ICML 2018. <a href="https://arxiv.org/abs/1801.01290">arXiv:1801.01290</a></li> <li>Haarnoja et al., <em>Soft Actor-Critic Algorithms and Applications</em>, 2019 — the automatic temperature tuning. <a href="https://arxiv.org/abs/1812.05905">arXiv:1812.05905</a></li> </ul>]]></content><author><name></name></author><category term="reinforcement-learning-for-control"/><category term="reinforcement-learning"/><category term="sac"/><category term="td3"/><category term="ddpg"/><category term="actor-critic"/><summary type="html"><![CDATA[DDPG, TD3, and SAC are usually presented as three algorithms. They're better understood as one algorithm and two rounds of debugging.]]></summary></entry></feed>